{
  "name": "cykloservis_app",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "npm-run-all -p dev:backend dev:frontend",
    "dev:backend": "cd backend && npm start",
    "dev:frontend": "cd frontend && npm start"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "npm-run-all": "^4.1.5",
    "react-scripts": "^5.0.1",
    "web-vitals": "^5.0.3"
  }
}
