// DUPLICATE REMOVED: original mechanic dashboard moved to mechanic/pages/MechanicDashboard.js
// Keeping empty stub so build does not break if something still imports it accidentally.
export default null;
